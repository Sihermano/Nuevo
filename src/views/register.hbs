<div class="col-md-10">
    <div class="card">
        <div class="card-header">
            <h1>Analytics</h1>
        </div>
        <div class="card-body">
            {{#if analytics}}
            <h1>Hay datos perro</h1>
            {{else}}
            <h1>No hay datos</h1>
            {{/if}}
        </div>
    </div>

</div>